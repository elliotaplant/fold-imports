{
  "name": "fold-imports",
  "main": "./lib/fold-imports",
  "version": "0.0.1",
  "description": "Easily fold `import` lines. Useful for .js(x) and .ts(x) files",
  "keywords": [
  ],
  "repository": "https://github.com/elliotaplant/fold-imports",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  }
}
